require_relative '../lib/dark_trader.rb'

describe "collect sign" do 
	it "says that we collect signs" do 
		expect(sign).to be_an_instance_of(Array)
  end
end

describe "collect sign" do 
	it "says that we collect signs" do 
		expect(price).to be_an_instance_of(Array)
  end
end